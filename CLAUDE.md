# The Lonelands

A Python/tcod Middle-earth roguelike built around The One Ring (TOR) tabletop mechanics.

## Agent skills

### Issue tracker

Issues and PRDs live as GitHub issues in `JonFarber81/the_lonelands`, managed via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Default canonical triage roles (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: one `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
